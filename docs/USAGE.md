# Usage Cheatsheet

- Caesar encrypt:
  `python -m src.cli encrypt --algo caesar --in "Hello World" --shift 3`
- Base64 decrypt:
  `python -m src.cli decrypt --algo base64 --in "SGVsbG8="`
- Launch GUI:
  `python -m src.cli --gui`


